import React from 'react';
import { Typography, Box } from '@mui/material';

const NotFoundPage = () => {
  return (
    <Box
      display="flex"
      flexDirection="column"
      justifyContent="center"
      alignItems="center"
      p={2}
    >
      <Typography variant="h3">NotFoundPage</Typography>
    </Box>
  );
};

export default NotFoundPage;
